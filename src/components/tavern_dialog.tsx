import { ReactHTMLElement } from "react";
import React from "react";

export default class TavernDialog {
    
    public next(): React.DetailedReactHTMLElement<HTMLDivElement, {
        return React.createElement('div');
    }
}